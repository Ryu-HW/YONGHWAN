package a1114;

public class MaxValue2 {
    public static void main(String[] args) {
        // int num1 = 12, num = 9;
        // int s = num1 > num ? num1 : num;
        // System.out.println(s);



        int score = 76;
        char g = (score > 90) ? 'A' : (score > 80) ? 'B' : (score > 70) ? 'C' : 'D';
        System.out.println(g);
    }
}
