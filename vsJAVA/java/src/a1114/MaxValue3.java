package a1114;

public class MaxValue3 {
    public static void main(String[] args) {
        // int num1 = 12, num = 9;
        // int s = num1 > num ? num1 : num;
        // System.out.println(s);



        int age = 15;
        String g = (age >= 20) ? "성인" : (age >= 13) ? "청소년" : (age >= 0) ? "어린이" : "사람이 아닙니다.";
        System.out.println(g);
    }
}
